# Frontend

React app for the Foreign Investment Tracker. See the repository root [README.md](../README.md) for the full project.

```bash
npm install
npm start          # development
npm run build      # production bundle
```

For production, set `REACT_APP_API_URL` to your backend base URL (no trailing slash on the env value is fine; the app builds URLs as needed).
